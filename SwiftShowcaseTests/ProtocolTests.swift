//
//  ProtocolTests.swift
//  Swift Showcase
//
//  Created by Sergio on 04/06/2014.
//  Copyright (c) 2014 Xebia IT Architects. All rights reserved.
//

import Foundation
import XCTest

/*
class ProtocolTests: XCTestCase {
    
    func testSquareArea() {

        var square1 = Square(size: 1)
        var square2 = Square(size: 2)
        
        XCTAssertEqual(square1.area(), 1, "square of 1 area should be 1")
        XCTAssertEqual(square2.area(), 4, "square of 2 area should be 4")
    }
    
    func testCircleArea() {

        var circle1 = Circle(radius: 1)
        var circle2 = Circle(radius: 2)
        
        XCTAssertEqual(circle1.area(), 1 * 1 * PI, "circle of 1 area should be \(PI)")
        XCTAssertEqual(circle2.area(), 2 * 2 * PI, "circle of 2 area should be \( 2 * 2 * PI)")
    }
    
    func testComputeShapesArea() {

        let shapes: Shape[] = [
            Square(size: 3),
            Circle(radius: 5)
        ]
        var expected = (3 * 3) + (5 * 5 * PI)
        
        var result = computeShapesArea(shapes)
        
        XCTAssertEqual(result, expected, "Computed area \(result) should be \(expected)")
    }
}
*/
